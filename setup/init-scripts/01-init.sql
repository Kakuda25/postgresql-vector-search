-- ============================================
-- 拡張機能の有効化
-- ============================================

-- pg_trgm: 全文検索・類似度検索用（あいまい検索に必要）
-- 商品名や説明文の部分一致検索を高速化する
CREATE EXTENSION IF NOT EXISTS "pg_trgm";

-- vector: ベクトル検索用（AI埋め込みベクトル用）
-- 商品の意味的類似性をベクトル距離で検索するために必要
CREATE EXTENSION IF NOT EXISTS "vector";


